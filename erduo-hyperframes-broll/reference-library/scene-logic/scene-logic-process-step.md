# 场景逻辑原子：过程步骤（示例，指导用，不强制）

用途：将“发生了什么→变成了什么”的过程可视化，而非重复文本。
适用：教学流程、系统链路、产品步骤说明。

- 结构意图：每个镜头只承载一个过程阶段。
- 关键动作：`开始状态 → 转换动作 → 结果状态` 三段闭环优先。
- 与组件协作：过程节点尽量与轻量组件联动，避免过度动画。

该原子只用于判断镜头是否可映射到关系变化，不替代素材路由规则。
