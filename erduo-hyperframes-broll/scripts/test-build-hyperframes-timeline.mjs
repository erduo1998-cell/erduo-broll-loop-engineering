import test from 'node:test';import assert from 'node:assert/strict';import {buildHyperframesTimeline} from './build-hyperframes-timeline.mjs';
const coverage={schema_version:1,plan_sha256:'a'.repeat(64),briefs_sha256:'b'.repeat(64),timeline:{start_ms:1000,end_ms:6000,duration_ms:5000},shot_count:2,coverage:{complete:true,coverage_basis_points:10000},windows:[{shot_id:'S001',start_ms:1000,end_ms:4000,duration_ms:3000,primary_compositing:'fullscreen'},{shot_id:'S002',start_ms:4000,end_ms:6000,duration_ms:2000,primary_compositing:'native-base-with-overlay'}]};
test('maps exact master global time to per-shot local zero time',()=>{const out=buildHyperframesTimeline(coverage);assert.deepEqual(out.clips.map(c=>[c.master_start_sec,c.master_duration_sec,c.local_start_sec,c.local_duration_sec]),[[1,3,0,3],[4,2,0,2]]);assert.deepEqual(out.master,{start_sec:0,visual_start_sec:1,duration_sec:6});});
test('rejects gaps, unverified coverage and unstable windows',()=>{for(const mutate of [v=>v.coverage.complete=false,v=>v.windows[1].start_ms=4001,v=>v.windows[0].shot_id='S009']){const v=structuredClone(coverage);mutate(v);assert.throws(()=>buildHyperframesTimeline(v));}});
