# 风格原子：缓动模式分类

用途：按动作角色选择缓动语义，防止所有元素使用同一种曲线。

- 适用：有进入、退出、强调、连续推进或状态切换的镜头。
- 约束：进入、退出、强调不能混用同一语义；不直接写裸 `cubic-bezier`。
- 验收：每个动效在 receipt 中声明 easing role，并解析为宿主可执行值。

外部吸收边界：只吸收“动作角色对应曲线类型”的方法，不复制库实现。
