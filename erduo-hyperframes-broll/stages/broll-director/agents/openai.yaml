interface:
  display_name: "B-roll Director"
  short_description: "Compile the script-only v3 director contract and deterministic policy receipt."
  default_prompt: "Use $broll-director to bind actual SRT bytes and canonical authoring artifacts; return the director-phase production contract, deterministic policy receipt, and bounded stage envelope."
