interface:
  display_name: "Legacy HyperFrames Delivery"
  short_description: "Inspect legacy HyperFrames delivery records."
  default_prompt: "Use $broll-render to inspect a pre-v0.9 HyperFrames delivery record and return a read-only recovery report."

policy:
  allow_implicit_invocation: false
