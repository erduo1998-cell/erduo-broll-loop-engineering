# video-shotcraft 样式：Real Screenshots for Existing Pages; Hand-Built UI Gated

来源：video-shotcraft 批量审计的脱敏抽象原子。具体证据见 handoff/VIDEO-SHOTCRAFT-*.json；本文件不复制 Remotion TSX、示例素材或音频。

- 用途：Any shot showing existing product pages MUST use real headless-browser screenshots (full-page 2x texture + per-element cutout + layout.json). Hand-built UI permitted ONLY for non-replica scenes (abstract openings, brand segments, standalone demo com
- 语义：Any shot showing existing product pages MUST use real headless-browser screenshots (full-page 2x texture + per-element cutout + layout.json). Hand-built UI permitted ONLY for non-replica scenes (abstract openings, brand segments, standalone demo components) with publication-grade quality gate. Failure → fall back to screenshot route.

边界：这是 advisory atom。用户 design、真实素材、当期语义优先；采用或拒绝都必须写入 reference_atom_trace。
