# 风格原子：静态对比（示例，指导用，不强制）

用途：当镜头切换过快或语义转折过密时，降低视觉噪声，保留可读性。
适用：数据解释、案例串联、结论收束。

- 语义指导：保留 1–2 个明确焦点，降低额外装饰元素密度。
- 结构规则：每个镜头最多保留一个主方向（例如左-右或下-上）。
- 与静帧融合：检测到人物较活跃时，优先把强调层放在人像安全区外侧。

该建议只说明“可优先尝试”，不要求每镜都应用。
