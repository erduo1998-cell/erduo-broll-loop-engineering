interface:
  display_name: "B-roll Runtime Planner"
  short_description: "Plan evidence-backed per-shot backends"
  default_prompt: "Use $broll-runtime-plan after Direction to generate and validate one deterministic runtime plan."
