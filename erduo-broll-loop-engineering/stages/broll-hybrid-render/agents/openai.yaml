interface:
  display_name: "Legacy Hybrid Delivery"
  short_description: "Inspect legacy hybrid delivery records."
  default_prompt: "Use $broll-hybrid-render to inspect a pre-v0.9 hybrid delivery record and return a read-only recovery report."

policy:
  allow_implicit_invocation: false
