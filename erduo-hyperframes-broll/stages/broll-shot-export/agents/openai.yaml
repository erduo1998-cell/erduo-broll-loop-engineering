interface:
  display_name: "B-roll Shot Export"
  short_description: "Optionally cut per-shot files from the technically verified master."
  default_prompt: "Use $broll-shot-export only after an explicit user request, byte-verified integration with the current integration-delivery receipt and a passing final technical verify; cut from the exact master without rerendering."
