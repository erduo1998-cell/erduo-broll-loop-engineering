export class InputTimeGateError extends Error { constructor(code,message){super(message);this.name='InputTimeGateError';this.code=code;} }
export function validateInputTimeGate({mode,timeline,control_media=null}){if(!['talking-head','faceless'].includes(mode)||!timeline||!Number.isSafeInteger(timeline.end_ms)||timeline.end_ms<=0)throw new InputTimeGateError('invalid_timeline','Authoritative SRT timeline is invalid.');if(mode==='talking-head'){if(!control_media?.video?.primary||!Number.isSafeInteger(control_media.duration_ms)||control_media.duration_ms<=0)throw new InputTimeGateError('control_media_invalid','Talking-head source video is invalid.');if(timeline.end_ms>control_media.duration_ms)throw new InputTimeGateError('srt_exceeds_media','SRT timeline exceeds source media duration.');}return{mode,timeline_end_ms:timeline.end_ms,control_duration_ms:control_media?.duration_ms??null};}
