# 场景逻辑原子：一镜一概念

用途：防止一个镜头同时解释多个抽象概念，导致文字堆叠和画面不可读。

- 适用：默认适用于所有正式镜头。
- 约束：每镜只承载一个主判断；辅助信息最多两条，且必须服务主判断。
- 验收：shot plan 的 `semantic_visual_contract` 必须能用一句话说清该镜头看什么。

外部吸收边界：来自 headless component 的“单职责”思想，只转成镜头约束。
