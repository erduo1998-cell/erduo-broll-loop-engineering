interface:
  display_name: "Legacy Remotion Delivery"
  short_description: "Inspect legacy Remotion delivery records."
  default_prompt: "Use $broll-remotion-render to inspect a pre-v0.9 Remotion delivery record and return a read-only recovery report."

policy:
  allow_implicit_invocation: false
