# video-shotcraft 质量门：Demo Source Code Must Be Read Before Shot Card Use

来源：video-shotcraft 批量审计的脱敏抽象原子。具体证据见 handoff/VIDEO-SHOTCRAFT-*.json；本文件不复制 Remotion TSX、示例素材或音频。

- 用途：Shot cards = semantics + parameter tables. demos/<card>/ source code = tuned parameter truth (easing, timing ratios, mask timing, known-pitfall workarounds). Writing fresh = discarding all tuning. Card's命门 parameters不得降档.

边界：这是 advisory atom。用户 design、真实素材、当期语义优先；采用或拒绝都必须写入 reference_atom_trace。
