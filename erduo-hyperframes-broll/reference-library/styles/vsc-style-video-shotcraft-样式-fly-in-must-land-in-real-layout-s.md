# video-shotcraft 样式：Fly-In Must Land in Real Layout Slot

来源：video-shotcraft 批量审计的脱敏抽象原子。具体证据见 handoff/VIDEO-SHOTCRAFT-*.json；本文件不复制 Remotion TSX、示例素材或音频。

- 用途：Elements flying in must embed into page layout (snap to real grid position, extend scroll flow). Floating above page = fake. Use layout.json coordinates as ground truth.

边界：这是 advisory atom。用户 design、真实素材、当期语义优先；采用或拒绝都必须写入 reference_atom_trace。
