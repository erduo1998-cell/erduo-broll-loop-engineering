interface:
  display_name: "B-roll Render and Delivery"
  short_description: "Render and technically verify one final master."
  default_prompt: "Use $broll-render to load the official hyperframes Skill, run same-environment doctor and check, obtain preview approval, and verify one final render."
