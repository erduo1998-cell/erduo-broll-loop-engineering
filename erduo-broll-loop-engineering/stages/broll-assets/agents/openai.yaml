interface:
  display_name: "B-roll Assets"
  short_description: "Freeze only material the shots actually need."
  default_prompt: "Use $broll-assets to inspect user media and shot-specific material needs, then conditionally freeze sourced or generated media and fonts with provenance and fusion geometry."
