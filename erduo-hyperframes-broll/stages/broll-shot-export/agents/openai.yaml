interface:
  display_name: "B-roll Shot Export"
  short_description: "Cut requested shots from the verified master."
  default_prompt: "Use $broll-shot-export to cut only explicitly requested shot windows from the verified final master without rerendering."
